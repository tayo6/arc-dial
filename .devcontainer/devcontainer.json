{
  "name": "arc-dial",
  "image": "mcr.microsoft.com/devcontainers/go:1.23-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev build-essential pkg-config xvfb && go install github.com/wailsapp/wails/v2/cmd/wails@latest && echo 'export PATH=$PATH:$HOME/go/bin' >> ~/.bashrc",
  "forwardPorts": [34115],
  "portsAttributes": {
    "34115": { "label": "Wails dev server", "onAutoForward": "openBrowser" }
  },
  "customizations": {
    "vscode": { "extensions": ["golang.go"] }
  }
}